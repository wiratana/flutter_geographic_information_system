class RoadUnit{
  RoadUnit(this.id, this.paths, this.id_village, this.code, this.name, this.length, this.width, this.composition, this.condition, this.type, this.description);

  final id;
  var paths;
  var id_village;
  var code;
  var name;
  var length;
  var width;
  var composition;
  var condition;
  var type;
  var description;
}